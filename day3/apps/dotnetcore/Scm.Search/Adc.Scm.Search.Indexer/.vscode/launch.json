{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Day3 - Launch Search Indexer Function",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}